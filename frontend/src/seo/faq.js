import en from '../content/faq-2026.en.json' with { type: 'json' }
import nl from '../content/faq-2026.nl.json' with { type: 'json' }

// The supplied FAQPage files are the single source for visible answers and JSON-LD.
export const faqByLanguage = { en, nl }
